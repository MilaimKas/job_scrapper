Simple app to scrap job offers from the german job center API (arbeitsagentur).
Default settings for project manager in the renewable sector in North Germany.

Single html file.
Deploy on Netlify.
The API is called via a proxy defined in netlify/functions.

